/**
 * EnrollmentDto
 * @typedef {Object} EnrollmentDto
 * @property {String} id
 * @property {String} studentId
 * @property {String} courseId
 * @property {Number} netFees
 * @property {Number} discount
 * @property {Date} enrolledOn
 * @property {Date} createdAt
 * @property {Date} updatedAt
 */

/**
 * EnrollmentController
 * @typedef {import('./enrollment.controller')} EnrollmentController
 */

/**
 * EnrollmentService
 * @typedef {import('./enrollment.service')} EnrollmentService
 */

/**
 * EnrollmentValidator
 * @typedef {import('./enrollment.validator')} EnrollmentValidator
 */
