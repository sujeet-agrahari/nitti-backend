/**
 * CourseDto
 * @typedef {Object} CourseDto
 * @property {String} id
 * @property {String} name
 * @property {Number} duration
 * @property {Number} price
 * @property {Number} discount
 * @property {Date} createdAt
 * @property {Date} updatedAt
 */

/**
 * CourseController
 * @typedef {import('./course.controller')} CourseController
 */

/**
 * CourseService
 * @typedef {import('./course.service')} CourseService
 */

/**
 * CourseValidator
 * @typedef {import('./course.validator')} CourseValidator
 */
